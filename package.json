{
  "name": "night-market-autobattler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "typecheck": "tsc --noEmit && tsc -p packages/sim/tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "stress": "node --import tsx scripts/stress.ts",
    "fixtures": "node --import tsx scripts/export-fixtures.ts",
    "smoke": "node --import tsx scripts/smoke.ts",
    "check": "npm run typecheck && npm run lint && npm test && npm run build",
    "format": "prettier --write src packages scripts tests docs README.md package.json tsconfig.json vite.config.ts eslint.config.js index.html .prettierrc.json",
    "format:check": "prettier --check src packages scripts tests docs README.md package.json tsconfig.json vite.config.ts eslint.config.js index.html .prettierrc.json"
  },
  "dependencies": {
    "ajv": "8.17.1",
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "@eslint/js": "9.35.0",
    "@types/node": "22.18.6",
    "eslint": "9.35.0",
    "playwright": "1.55.0",
    "prettier": "3.8.1",
    "tsx": "4.20.5",
    "typescript": "5.9.2",
    "typescript-eslint": "8.42.0",
    "vite": "7.1.5",
    "vitest": "3.2.4"
  }
}
